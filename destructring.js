// Destructing --> pega um valor de um array e coloca numa variavel.
// // sempre que os colchetes estiverem do lado esquerdo do sinal de igual, 
// sig que estamos pegando os valores do array e trazendo para as variaveis.

const arr = [15, 22, 37]

const [n1, ,n2] = arr
console.log(n1)
console.log(n2)